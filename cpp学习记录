1.if（逻辑表达式）
{
    语句体1；	
}
 else
{
    语句体2；
 }


2.whlie（）
{
    
}


3.for（初始化；逻辑表达式；更改条件）
{
    循环体
}

4.
switch（变量）
{
    case  常量1；语句体1；break；
    case  常量2；语句体2；break；
    （...）
    default：语句体x；break；
}


变量：
#include <iostream>
using namespace std;

int main()
{
	int a = 10;

	cout << "a = "<<a << endl;
	/*
	多行注释
	*/
	system("pause");

	return 0;
}
/*
变量存在的意义：方便管理内存
变量创建的语法: 数据类型 变量名 = 变量初始值;
                    int a = 10;
*/



常量：用于记录程序中不可更改的数据
1.#define宏常量：#define 常量名 常量值//通常在文件上方定义，表示一个常量
2.const修饰的变量：const 数据类型 常量名 = 常量值//通常在变量定义前加关键字const，修饰该变量为常量，不可修改
{
const int month = 12;
mouth = 24 //错误，const修饰的变量也称为常量，常量一旦修改就会报错
}

创建变量： 数据类型 变量名 = 变量初始值
不要用关键字给变量或者常量起名

标识符命名规则：
标识符不能是关键字
标识符只能由字母、数字、下划线组成
第一个字符必须为字母或下划线
标识符中字母区分大小写
//给变量起名的时候最好能起到见名知意的效果

cpp规定在创建一个变量或者常量的时候，必须要指定出相应的数据类型，否则无法给变量分配内存

数据类型：
1、整型
short/int/long/long long 占用内存空间不同<<取值范围不同
-32768~32767

2.sizeof关键字：用于统计数据类型所占内存大小
语法sizeof( 数据类型/变量 )

3.实型（浮点型）  用于表示小数
1.单精度float  占用4字节 7位有效数字  一般在数字后面多加个字母f   float f1 = 3.14f;
2.双精度double   8      15~16                                    double d1 = 3.14;
默认情况下输出一个小数会显示出6位有效数字 eg:3.14159
3e2=3*10^2

4.字符型  用于显示单个字符  字符型变量仅占用一个字节 sizeof(char)<<1
语法：char ch = 'a';
单引号、单引号内只能有一个字符，不可以是字符串
字符型变量并不是把字符本身放到内存中存储，而是将对应的ASCII编码放入到存储单元中
强转：cout << (int)ch << endl;
a  97
A  65

5.转义字符  用于一些不能显示出来的ASCII字符
常用的：\n  \\  \t
\n:换行      ASCII码值010
\\:代表一个反斜字符\    092
\t:水平制表，跳到下一个TAB位置（可以整齐输出数据）  009 
